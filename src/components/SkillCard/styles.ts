import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  skillButton: {
    backgroundColor: "#1f1e25",
    padding: 15,
    borderRadius: 50,
    alignItems: "center",
    marginBottom: 8,
  },
  skillText: {
    color: "#fff",
    fontWeight: "bold",
    fontSize: 22,
  },
});
